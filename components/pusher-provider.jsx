"use client";

/**
 * PusherProvider — stub. The warmup triage service does not use Pusher.
 * Real-time updates aren't required by the spec.
 */
export function PusherProvider({ children }) {
  return children;
}